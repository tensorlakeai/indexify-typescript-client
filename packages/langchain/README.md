# Indexify Langchain Retriever

This package contains the indexify langchain retriever so that you can build langchain LLM applications with indexify.

### Install
```bash
npm install @getindexify/retriever
```